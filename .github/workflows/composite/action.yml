inputs:
  name:
    description: 'Build and push image to Amazon ECR'
runs:
  using: "composite"
  steps:
    - name: Test
      run: |
        printenv | grep INPUT
        echo $INPUT_TEST_SECRET
      shell: bash
